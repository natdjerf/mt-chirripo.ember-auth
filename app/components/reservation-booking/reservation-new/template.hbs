<h1>Plan Your Trip</h1>

<h4>Permits &amp; Lodging:</h4>
<div>
  <p>Park Entry Permit: $15 USD per person per day</p>
  <p>Lodging Accomodations: $35 USD per person per night</p>
  <p>Includes sheet, pillow, &amp; sleeping bag</p>
</div>
<div>
<form {{action "submit" reservation on="submit"}}>
    {{input type="number"
            placeholder="Permit Quantity"
            class="input-group form-control"
            value=reservation.permitQuantity}}
    {{input type="number"
            placeholder="Lodging Quantity"
            class="input-group form-control"
            value=reservation.lodgingQuantity}}
    {{bootstrap-datepicker
      autoclose=true
      date=mydate
      todayHighlight=true
      valueFormat='YYYY-MM-DD'
      placeholder="Permit Start Date"
      class="input-group form-control"
      value=reservation.permitStartDate}}
    {{bootstrap-datepicker
      autoclose=true
      startDate=permitStartDate
      date=mydate
      valueFormat='YYYY-MM-DD'
      placeholder="Permit End Date"
      class="input-group form-control"
      value=reservation.permitEndDate}}

  <h4>Total Cost:</h4>
  <div>${{reservation.totalCost}}</div>

  <input type="submit" class="btn btn-succss">
</form>
</div>
