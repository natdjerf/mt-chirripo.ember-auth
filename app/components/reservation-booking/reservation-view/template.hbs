<h2>Upcoming Reservations</h2>

<div>
    <p>Number of Permits:
      <span class="permit_quantity" contenteditable="true">{{reservation.permit_quantity}}</span>
      {{!-- <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> --}}
    </p>
    <p>Number of Lodging Reservations:
      <span class="lodging_quantity" contenteditable="true">{{reservation.lodging_quantity}}</span>
      <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
    </p>
    <p>Dates:
      {{!-- <span class="permit_start_date">{{bootstrap-datepicker date=mydate valueFormat='YYYY-MM-DD'}}</span> --}}

      <span class="permit_start_date" contenteditable="true">{{reservation.permit_start_date}}</span>
      <span class="permit_end_date" contenteditable="true">{{reservation.permit_end_date}}</span>
    </p>

    <p>Total Cost:
      <span class="total_cost" contenteditable>{{reservation.total_cost}}</span>
      {{!-- <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> --}}
    </p>
</div>

<button {{action "deleteReservation"}} class="btn btn-danger">Cancel This Reservation</button>
<button {{action "modifyReservation"}} class="btn btn-success">Change This Reservation</button>
