import { calculateTotal } from 'mt-chirripo.ember-auth/helpers/calculate-total';
import { module, test } from 'qunit';

module('Unit | Helper | calculate total');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = calculateTotal([42]);
  assert.ok(result);
});
